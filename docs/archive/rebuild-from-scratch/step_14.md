# Step 14. 최종 완료 상태 스냅샷

이 문서는 2026-03-06 기준 실제 완료 상태를 요약한 마감 메모다.  
재구축 문서의 기준선이 바뀌지 않도록, 최종 값과 잔여 예외를 여기에 고정한다.

## 최종 수치

- 전체 파일그룹: `413`
- 드라이버측(`25`): `361`
- 조수석측(`26`): `52`
- `downloaded` 파일그룹: `411`
- `error` 파일그룹: `2`
- 다운로드 파일 수: `24,793`
- 제외 파일 수: `148`
- 제외 폴더 수
  - `photo-folder`: `504`
  - `video-folder`: `416`

## 최종 범위 판정

최종 완성본은 아래를 다운로드 대상으로 본다.

- `DATA*`
- `REPORTS`
- `ROOT` 파일

최종 완성본은 아래를 제외 대상으로 본다.

- `PHOTOS`
- `VIDEO`
- 사진 확장자 파일
- 영상 확장자 파일

## 최종 raw 산출물 구조

현재 raw 산출물 기준:

- `PHOTOS` 디렉터리 개수: `0`
- `VIDEO` 디렉터리 개수: `0`

즉, 빈 폴더에서 다시 만들 때도 같은 결과가 나와야 한다.

## 허용된 예외

### 1. 정상 zero-byte 원본 1건

- filegroup `1726`
- 경로: `ROOT\TestEnvironmentalData.csv`
- `size_label`: `0 bytes`
- 로컬 파일 길이 `0`은 정상

### 2. 사이트 `HTTP 400` 예외 2건

- filegroup `2344`
  - `DATA\EXCEL\CEN1438 Driver Door`
- filegroup `7961`
  - `DATA\EXCEL\5A65DF20`

이 두 항목은 반복 재시도 후에도 서버가 `HTTP 400`을 반환했다.  
따라서 현재 완료 판정은 "다운로드 가능한 나머지 전부 확보 + 이 예외 2건 기록" 상태다.

## 완료 판정 문구 권장안

다른 문서나 인수인계 메모에 아래 문장을 써도 된다.

`IIHS small-overlap driver/passenger 데이터 413개 filegroup 처리를 완료했으며, 411개는 정상 완료, 2개는 사이트 측 HTTP 400 예외로 기록했다. 최종 산출물은 PHOTOS/VIDEO를 제외하고 DATA, REPORTS, ROOT 파일 중심으로 정리되어 있다.`
